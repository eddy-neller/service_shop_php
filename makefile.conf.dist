# Copier en makefile.conf (ignore par Git).
YELLOW := \033[0;33m
GREEN := \033[0;32m
RED := \033[0;31m
RESET := \033[0m

DOCKER = docker compose
APP = $(DOCKER) exec app

# Nombre d'instances HTTP du service `app` lancees par `make up`.
APP_REPLICAS = 3
